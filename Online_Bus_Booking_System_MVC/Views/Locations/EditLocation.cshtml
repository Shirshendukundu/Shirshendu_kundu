@model Online_Bus_Booking_System_DAO.Location
@{
    ViewData["Title"] = "Delete Location";
}
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>@ViewData["Title"]</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
    <style>
        body {
            background: linear-gradient(to right, #ff6b6b, #f6d365);
            color: white;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: 'Arial', sans-serif;
        }

        .container {
            margin-top: 50px;
        }

        .card {
            background: rgba(0, 0, 0, 0.75);
            color: white;
            border-radius: 15px;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.5);
        }

        .card-header {
            background: #dc3545;
            color: white;
            border-top-left-radius: 15px;
            border-top-right-radius: 15px;
            text-align: center;
            padding: 20px 0;
            font-size: 1.5em;
        }

        .btn-danger {
            background-color: #dc3545;
            border: none;
            padding: 10px 20px;
            font-size: 1.1em;
            transition: transform 0.3s;
        }

            .btn-danger:hover {
                background-color: #c82333;
                transform: scale(1.05);
            }

        .btn-secondary {
            margin-top: 10px;
        }

        .form-group {
            margin-bottom: 20px;
        }

        dl.row {
            margin-top: 20px;
        }

        dt {
            font-weight: bold;
        }

        hr {
            border-top: 1px solid #fff;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card">
                    <div class="card-header">
                        <h3>@ViewData["Title"]</h3>
                    </div>
                    <div class="card-body">
                        <h3>Are you sure you want to delete this?</h3>
                        <h4>Location Details</h4>
                        <hr />
                        <dl class="row">
                            <dt class="col-sm-2">@Html.DisplayNameFor(model => model.LocationId)</dt>
                            <dd class="col-sm-10">@Html.DisplayFor(model => model.LocationId)</dd>
                            <dt class="col-sm-2">@Html.DisplayNameFor(model => model.TerminalName)</dt>
                            <dd class="col-sm-10">@Html.DisplayFor(model => model.TerminalName)</dd>
                            <dt class="col-sm-2">@Html.DisplayNameFor(model => model.City)</dt>
                            <dd class="col-sm-10">@Html.DisplayFor(model => model.City)</dd>
                            <dt class="col-sm-2">@Html.DisplayNameFor(model => model.Province)</dt>
                            <dd class="col-sm-10">@Html.DisplayFor(model => model.Province)</dd>
                        </dl>
                        <form asp-action="DeleteLocation">
                            <div class="form-group">
                                <input type="hidden" asp-for="LocationId" />
                                <input type="submit" value="Delete" class="btn btn-danger" />
                            </div>
                            <a asp-action="Default" class="btn btn-secondary">Cancel</a>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
    @section Scripts {
        <partial name="_ValidationScriptsPartial" />
    }
</body>
</html>
